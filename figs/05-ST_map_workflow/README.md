# Figure 5: ST-map workflow

The current `fig/st_map_workflow_velocity_selected.png` is the manually
recomposed final figure used by the manuscript. The four files in `data/` are
the retained panel-level source artifacts and `ST_line_1.json` is associated
metadata.

The former `make_st_map_workflow.py` has been moved to `archive/` because it is
an older velocity/ST composition script and cannot regenerate the current
manually recomposed figure. No current end-to-end figure regeneration command
is claimed for this package.
